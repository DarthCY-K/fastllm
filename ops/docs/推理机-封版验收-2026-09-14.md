# 推理机封版验收记录（2026-09-14 晚）

> 覆盖当日四连变更后的综合验收：r2 升级 → 间隔4 → DFlash 尾块修复 → backbone TP force。生产经"自启翻转 + 重启演练"后完成以下全部验收。

## 0. 自启翻转与重启演练

- `systemctl enable fastllm-qwen38-tp4` + `disable qwen38-0.2x-tp4`（此前重启会悄悄回落旧 vLLM）。
- 19:22:10 发起 `systemctl reboot` → **140s 后 SSH 回线**；fastllm 自动起（PID 1912，SubState=running），旧 vLLM 未起（inactive）。
- 重启后指纹齐全：`1M session` / `Yarn gate` / `TP prepared: 5 paired MLPs` / `0 Traceback` / HTTP 401 ~25s。

## 1. 功能回环

| 项 | 结果 |
|---|---|
| 工具调用 | `finish=tool_calls`，`get_weather{city:Beijing}` ✓ |
| soak v3 | 新样本含 `seed=/skip=` 计数（首样本 seed=1256 skip=4）✓ |

## 2. 真实画像长输出

| 用例 | 输出 tokens | TTFT | 解码速率 | 备注 |
|---|---:|---:|---:|---|
| thinking 长输出（LRU 编码题） | 3550 | 0.11s | **134.6 tok/s** | 推理文本接受率低于计数类，属内容特性 |
| 32K 上下文长解码（count 1000） | 3892 | 33.9s（预填） | **219.9 tok/s** | md5 `fc3292b2e08d`；长解码无衰减 |

## 3. 1M 长上下文针测（关键项）

- 提示 **308,204 tokens**（针埋于深度 120K 处），**>262K 原生上限 → YaRN 外推路径**。
- 结果：正确答出 `X9J7-QUARTZ-3312`（`found: true`）。
- `seeded: tokens=308216, chunk=512`——>262K 的前缀种子缓存也正常入库。
- 预填 520.7s（均值 ~590 tok/s；尾段 435）；0 Traceback；显存正常。
- 备注：针测占用单流期间 soak 19:30 拍出现 `ok=0`（请求排队超时），属预期假警报，19:36 恢复。

## 4. 虾跑分抽查（走 subapi 真实链路）

- **总分 86.8 / 100**（段位顶级，P80）；8 科：全 95（记忆力 93.3）；**反思力 95**。
- 落于历史 8 连发带（84.6–87.8）内 → 当日三次转正**无质量回归**。
- 报告：https://paofen.cocoloop.cn/report/ses_1789386124795_oj5qbq

## 5. 结论

**封版通过。** 生产 = r2 + 间隔4 + 尾块修复 + backbone force，开机自启可用、1M 可用、工具可用、质量稳态。
后续可选（未做，另议）：预填 chunk 扫描、NCCL 微调、草稿 FP8、上游 PR/issue 提交（草案见《上游回馈草案-2026-09-14.md》）。
