# 推理机 FastLLM r3（上游 74d3638 合并）验收记录 — 2026-09-15

## 1. 合并（GitHub fork）

- 上游增量：`ztxz16/fastllm` `21650fae..74d3638`（7 提交：FP8 缓存初始化跨卡死锁 / MTP 验证图快照失效 / MTP 随机草稿拒绝采样统一 / Gumbel 融合草稿采样 / 草稿 token 回传减少 / MTP 草稿前缀图 / 设备算子生命周期泄漏）。
- 合并提交：**`3f19b5106b6c2d084a82932cca0920cee8196a9c`**（parents `b6c61cd77` + `74d363833`），分支 `DarthCY-K/fastllm:sm75-2080Ti`，ahead 15 / behind 0。
- 手法：GitHub API（控制器 `github.com:443` 不通、`api.github.com` 通）——临时 ref 指上游 tip → `POST /repos/.../merges` → 删临时 ref。
- 验证：4 个双侧同名文件（`fastllm-cuda.cuh`/`qwen3_5.h`/`fastllm-cuda.cu`/`qwen3_5.cpp`）本地三方合并结果 blob sha **全部等于** GitHub 合并树；树清点 = 上游新增 7 / 删 1 / 本地 129 新增 + 22 修改全保留。
- 回滚点：tag `pre-upstream-merge-20260915` = `b6c61cd7`。

## 2. 构建（AI-ReasoningCenter）

- 取料：`git fetch fork sm75-2080Ti` → `git archive` → `repo-r3`（2435 文件；4 文件 blob sha 与 GitHub 一致；`ALLOW_YARN_WITH_DFLASH`、`finalChunkDecodeSteps`、`default_effort` 标记齐）。
- 配方同 r2：`cmake -DUSE_CUDA=ON -DUSE_NUMAS=ON -DUNIT_TEST=ON -DCUDA_ARCH=75 -DCMAKE_CUDA_COMPILER=/usr/local/cuda-12.8/bin/nvcc -DCMAKE_CXX_COMPILER=/usr/bin/c++` + `CPATH=<fastllm-test-venv nccl include>` + `LIBRARY_PATH=fastllm-link-deps` + `make -j96 fastllm_tools`。
- 结果：08:47:29 → 08:52:28（5 分钟），`.so` 124,341,040B **md5 `50bac23857803f748ac926f8bdba4874`**；串计数 `ALLOW_YARN_WITH_DFLASH`=2、`proposal_q`=1、`selector_q`=1。python 层（qwen3coder_tool_parser.py）md5 `b2535575…` 与生产部署件一致。

## 3. 阶段验收（3 个窗口，自动恢复生产）

- 功能：401/200、模型列表、chat、tool_choice=required（get_weather/Beijing）、多轮 prefix 恢复 15360 tokens（ttft 0.2-0.4s）、count200 temp0 md5 与生产**逐字节相同** `0785ac9ffdae`、DFlash2 backbone TP force 生效（5 paired MLPs / 4 ranks）、desync=0、accept 92-95%。
- 尾块≤64 回归（收敛法构造，mod 512 = 16 / 62）：`long prefill cache seeded` 出现、desync=0、decode 222-226 tok/s（退化路径 ~52）；控制组 mod=384 正常。

## 4. 全量 A/B（同窗三明治：prod A → r3 → prod C）

thinking=on + reasoning_effort=medium、temp=0、max_tokens=256。客户端 TTFT/总量 + 引擎侧稳态 decode（`[Decode] alive = 1, pending = 0` 采样）双轨。

| 行 | TTFT prodA / r3 / prodC (s) | 引擎 decode prodA / r3 / prodC (tok/s) | 输出 md5 |
|---|---|---|---|
| 2048 digits cold | 1.85 / 1.85 / 1.87 | 217.7 / 222.1 / 214.5 | A=C=44d881b04d94，r3=16e17bd70046 |
| 2048 prose cold | 1.80 / 1.80 / 1.81 | 154.3 / 150.3 / 152.9 | 三者不同（散文随内容变） |
| 65536 digits cold | 63.18 / 63.10 / 63.58 | 214.9 / 207.9 / 211.4 | 三者 44d881b04d94（跨构建逐字节一致） |
| 65536 prose cold | 62.06 / 61.66 / 61.98 | 114.2 / 102.3 / 140.5 | 三者不同 |
| 131072 digits cold | 144.95 / 144.21 / 144.73 | 198.9 / 198.9 / 198.3 | 三者 44d881b04d94 |
| warm 行（同文重复） | 0.09-0.87（前缀恢复） | 与 cold 同档 | 与各自 cold 相同 |

- 预填/TTFT：r3 全档 ≤ prod（差 0.01→0.63s，<0.5%）。
- 解码：128K 逐平；2K digits r3 略高、2K prose 略低；64K digits −1.7~−3.3%（prod 自身 A/C 差 1.6%）→ 噪声边缘。
- **散文行不可用于回归判定**：prod 同构建 64K prose 两次 114.2 vs 140.5（+23%）；2K prose 亦有 ~19% 波动。
- 内存：100 请求 RSS 增长 prodA +20.0MB / r3 +20.4MB / prodC +31.9MB → 无差异（`74d363833` 在本规模不可分辨）。
- 日志：三窗口 seeded=8 / restored=104 / desync=0 对称；GPU 温度 68-74°C 三窗口一致。

## 5. 结论与产物

- 结论：**r3 无系统性回归**（所有偏差 ≤3.3%，散文行噪声可达 23%，128K 逐平；预填与内存逐平）；r3 另带 FP8 缓存死锁与算子生命周期泄漏两个上游修复。
- 产物：本地 `hermes/cache/etfp8-ab/upstream-20260915/`（上游 diff、树、合并模拟、`r3-scripts/` 全套脚本+JSON+日志）；盒子 `upgrade-test/`（`repo-r3`/`build-r3`/`overlay-r3`/`run_smoke_r3.sh`/`run_tail_r3b.sh`/`run_ab_r3.sh`/`r3_probe.py`/`r3_tail_probe2.py`/`ab_r3_suite.py`/`extract_blocks.py`）。

## 6. 转正（2026-09-15 09:59 → 10:01，用户决策）

- 切换脚本 `ops/deploy/switch_to_r3.sh`：预检（overlay `.so` md5=`50bac238…`、5 条门控串、`default_effort`、工具解析补丁 md5 `b2535575…`）→ 备份 venv ftllm（616M，`ftllm.backup-20260915-pre-r3`）→ `sync_tree.py` 160 copies / 0 deletes → venv 侧 md5 双比对（.so + fastllm_completion.py + qwen3coder_tool_parser.py 全部一致）+ import 冒烟 → `sudo systemctl restart fastllm-qwen38-tp4` → 401 就绪（~35s，实测窗口内完成）。
- 回滚脚本 `ops/deploy/rollback_r3.sh`（停服 → 备份包覆盖 → 重启 → 就绪检查；本次未触发，句柄实测完好：备份包 `.so` = `9c010b44…`）。
- 转正后验收（`post_switch_probe.py` + `r3_tail_probe2.py`，全 PASS）：无 key 401 / models 200 / chat smoke / 工具回环（get_weather, Beijing）/ count200 temp0 md5=`0785ac9ffdae`（与转正前生产逐字节一致）/ 两轮续写 ttft 0.41s；尾块 ≤64（mod 512 = 16、62）seeded 出现、desync=0、decode 221-227 tok/s（退化路径 ~52）、控制组 mod=384 为 210 tok/s。
- 启动行为指纹（新引擎）：`[fastllm-experimental] Yarn + external DFlash/DSpark draft explicitly allowed by FASTLLM_ALLOW_YARN_WITH_DFLASH`、`[Qwen3.5 DFlash2] enabled: layers=5, drafts_per_step=7, …, tp_devices=4, acceptance=exact(greedy)/rejection(selector_q,target_p)(sampling)`、`Per-session context window limit: 1048576`、`KV Cache Token limit: 1048576 (totalPages=8192)`、`TP prepared: 5 paired MLPs … 4 CUDA ranks`。
- 独立复核：unit `active`+`enabled`、MainPID 26842、`0.0.0.0:8080` 在听、venv `.so`=`50bac238…`、GPU 16.5-17.8 GB / 41-44°C；生产 launcher 与 argv JSON 未改动（仅 venv 内 ftllm 包原位替换）。
- 待办：把本记录 + `switch_to_r3.sh`/`rollback_r3.sh`/探针脚本入库 `ops/` 并打验收 tag `r3-prod-20260915`。
