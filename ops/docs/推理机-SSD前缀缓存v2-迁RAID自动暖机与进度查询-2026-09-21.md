# 推理机 SSD 持久前缀缓存 v2 — 迁 RAID0 / 自动暖机 / 进度查询（2026-09-21 夜）

## 背景（v1 遗留）
- 首请求"全量读取校验/索引重建"成本 ≈ 存储/295MB/s（28G→95–96s 实测），只在切换/回滚脚本里手动暖机，崩溃/掉电拉起的重启无暖机，成本暴露给首个真实请求（当天口径："裸重启后不要直接放真实流量"）。
- 存储放根盘（/home/ai-agent/prefix_ssd_prod，16G），配额 16GiB 顶格运行，每次新会话落盘都要触发配额淘汰维护。

## 本次变更（2026-09-21 夜，实测记录见 ops/evidence/ssd-v2-20260921/）
1. **存储迁 RAID0**：`/home/ai-agent/prefix_ssd_prod` → `/var/cache/lmcache/prefix_ssd_prod`（md127 = 2× Fanxiang S790 2TB RAID0，3.73TiB，clean）。现有 16G 存量已 `cp -a` 迁移，**原目录保留作回退**。旧 vLLM/LMCache 遗留 639G（objects/、tmp/）已清理回收（无服务/进程引用，最后写入 09-19）。
2. **配额**：`16GiB → 32GiB` 起步（launcher 一行；惰性化上线后可再放开）。
3. **挂载守卫**：launcher 检查 `/var/cache/lmcache` 是挂载点 + store 目录可写，否则**禁用 SSD**（防 RAID 缺失时静默写根盘）；systemd 单元加 `Wants=/After=var-cache-lmcache.mount`。
4. **自动暖机**：单元 `ExecStartPost=/home/ai-agent/ops/warmup_on_start.sh`（异步自守护、flock 互斥、写状态 JSON）→ **任何原因的重启**（含崩溃/掉电拉起）自动吸收首请求恢复成本，用户无感。
5. **暖机进度查询**：`/home/ai-agent/ops/warmup_status.py`（默认人类可读，`--json`、`--watch N`）；数据源 = systemd 状态 + `warmup_state.json` + 引擎侧 `recover-progress.json`（惰性化补丁上线后可用）。

## 生效与回退
- 本次 ops 变更**下次重启生效**；今夜已手动验证 `warmup_on_start.sh --run`（0s 就绪→暖机 rc=0）与 `warmup_status.py` 输出。
- 回退：launcher `.bak-pre-ssd-v2-20260921`；单元 `.bak-pre-warmup-20260921`；存储原目录 `/home/ai-agent/prefix_ssd_prod` 未动。

## 验证清单（待重启窗口执行）
- [ ] 重启后 `warmup_status.py` 显示 state=done、warmup_elapsed ≈ 存储/295MB/s（当前 16G ≈ 55s）
- [ ] launcher 打印 `[prod-launch] SSD prefix cache: /var/cache/lmcache/prefix_ssd_prod (quota=32GiB restore=always)`
- [ ] 引擎日志出现 `[Prefix SSD]` 身份/恢复行，指向新目录
- [ ] 跨重启恢复一条既有前缀（md5 一致）
