# 推理机 FastLLM r2 升级执行记录（2026-09-14）

- **对象**：AI-ReasoningCenter（4×2080Ti 22G，生产 = EfficientThink-FP8 + YaRN 1M + DFlash2，FastLLM）
- **一句话**：在 r1（57ecd5a+本地补丁，已于今晨转产并 soak）之上，叠加**上游 master 21650fa + 三个未合并 PR（#731/#732/#730）**，产出 **r2 候选构建**；冒烟与 A/B 对照通过，经 ab5/ab6 严谨复测后**已于 2026-09-14 14:43 转正为生产**（switch-r2，生产中断 ~30s；回滚脚本就绪）。

## 一、r2 组成

| 层 | 内容 |
|---|---|
| 上游 | master `21650fa`（含 #728 思考循环修复链：分布化草稿采样/链拒绝/Gumbel-max/随机草稿开关；#733 内容并入；`2d32320` 分页上传旧架构兼容；#729 等） |
| 本地 | Yarn×DFlash 门控补丁、packed-int8、`default_effort=medium` 修复（沿用） |
| PR | **#731** 分块预填尾块卡顿 + 草稿缓存失步自愈 + `AUTOWARMUP_SPARE_KV_MB`；**#732** 单请求错误不再杀进程（ServingModeScope）/worker 自连接防护/外部 MTP×mmproj 共存；**#730** qwen3.5 前缀快照（解码阶段记录、2 页间隔、共享缓冲） |
| 手法 | PR 以 `git apply --3way` 套用（repo 为合成历史，`refs/pull` 分支无法直接 merge）；4 处冲突逐一裁决（cpudevice include 取并集；acceptance 串取上游；#731 接受判定块弃用（上游已含同类修复）；#732 视觉 guard 与上游 VisionExecutorScope 并存） |

## 二、验证证据（均为实测）

- **构建**：`build-r2/tools/ftllm/libfastllm_tools.so`，123,309,712 B，md5 `2d0894a26082360d1d3734bf4b30047a`；gate 字符串保留（count=2）。
- **冒烟（SMOKE_DONE ready=1 prod=1，11:34:57–11:36:15）**：无 key 401；models 200；chat `UPGRADE_SMOKE_OK`；工具回环 `finish=tool_calls / get_weather / {city: Beijing}`；启动日志含新 `acceptance=exact(greedy)/rejection(selector_q,target_p)(sampling)`。
- **ab3 混合对照（现产 8080 vs r2 8081）**：质量三项（math/json/tool）双构建全过；短提示 TTFT 0.48–0.55s → 0.25–0.29s；DFlash 接受率 [83.6,64.8,45.3,…] → [77.3,58.6,41.4,…]/[71.9,…]（与"正确采样"方向一致）。
- **ab4 确定性对照（temp0 计数任务）**：

| 用例 | old | new | 输出 |
|---|---|---|---|
| d1 数 1–200 (t0) | tps 211.0 | tps 202.4 | md5 逐字节一致 `dacc241c7db8` |
| d2 数 1–200 (t0.8) | tps 215.5 | tps 211.6 | 一致 `dacc241c7db8` |
| d3 数 1–100 @32K 填充 (t0) | ttft 31.2s / tps 199.9 | ttft 33.5s / tps 209.1 | 一致 `b536474ba172` |

- **生产现状（14:43 转正后）**：r2 构建（.so md5 `2d0894a26082360d1d3734bf4b30047a`，见第三节转正记录）；active、NRestarts=0、401/200、chat/工具/公网中继端到端回环通过；行为指纹 `long prefill cache seeded: chunk=256`；GPU 19.5G/15.8G×3。

## 三、已知项 / 待办

- **d3 的 32K 冷预填 +7%：已复测并分解**（见《推理机-FastLLM-r2严谨复测总结-2026-09-14.md》）——真实来源为：① 服务冷启动后前 1~2 个长预填的瞬态 +2~2.8s；② #730 的 256 分块新默认。**r2 稳态（第 3 个预填起）31.23s ≈ 现产 31.1–31.4s，无稳态回归**；decode −1.5~2%；多轮续写恢复收益 −56% TTFT；q1 散文 temp0 输出存在随 256 分块的确定性差异（待评估）。
- 上游 issue #726（DFlash 缺 stream sync）仍未修复——长跑 soak 风险不变。
- soak.log 的 inactive/ok=0 采样为**授权中断**：11:35/11:39/11:44（r1 测试窗）、14:33（AB6 窗口）、14:43（r2 切换重启窗），非故障。
- **转正已执行（2026-09-14 14:43:38–14:44:12）**：`scripts/switch_to_upgrade_r2.sh`（预检 .so md5/四条 gate 串 → 备份 venv 包 629M → sync 160 文件 → venv 内 md5/import 复验 → 重启等待 → 行为指纹探针）；回滚 `scripts/rollback_upgrade_r2.sh`（恢复 `ftllm.backup-20260914-pre-r2` = r1 生产包）。基线：tag `r2-prod-20260914`、bundle（42MB）与清单在 `upgrade-test/artifacts/`。

## 四、产物索引

- 远端 `~/builds/upgrade-test/`：`repo`（分支 merge2，tag `r2-prod-20260914`）、`build-r2/`、`overlay-r2/`、`smoke-r2.log`、`ab3_{old,new}.json`、`ab4_{old,new}.json`、`scripts/run_{smoke,ab,ab4}_r2.sh`、`fetch-r2/`（上游 tar + PR diffs）；转正产物：`scripts/switch_to_upgrade_r2.sh`、`scripts/rollback_upgrade_r2.sh`、`switch-r2.log`、`artifacts/`（bundle + 清单 + 脚本副本）；严谨复测：`scripts/ab5_rigorous.py`、`ab6_rigorous.py`、`run_ab5.sh`、`run_ab6.sh`、`fastllm_test_launch_r2_snap16.py`、`ab5_*`、`ab6_B.json`、`stack-{b,b2,c}.log`。
- 本地 `hermes/cache/etfp8-ab/r2-20260914/`：全部脚本/结果/脱敏日志归档（ab5-ab6 子目录含解析/对比器）。
