# 推理机 FastLLM r9 上游全量合并 + PR#747 接入 — 2026-09-21

## 结论摘要
- **r9**（上游 13 提交全量）：分支 `r9-upstream-20260921` @ `15d53da0`；构建全绿，`so md5 ac56167602221ff4c5d8850930c00e07`。
- **r9b**（r9 + PR#747）：分支 `r9-747-20260921` @ `e5414da9`；构建全绿，`so md5 66f68772abe34a596bbe187addb0451a`；#747 自带 CPU 测试 4/4 通过。
- 冒烟窗口已执行（2026-09-21 10:39–10:42，三探针全绿；md5 与 r8 基线逐位一致，见下节）。
- 生产未动：仍为 r8（so `1c7fc3f3`）。候选分支本地已提交；**推送 fork 待执行**（非交互会话无 GitHub 写凭据）。

## 上游内容（相对 r8 基线 cb465fe4，共 13 提交，master 2236e001）
- NCCL 启动自检定稿（`9be23bc6` + `2236e001`：fail-fast、超时 `FASTLLM_NCCL_INIT_TIMEOUT_MS`、回归测试）——取代我们 `3ad12f3d` 的初版 pick。
- BF16 八行小投影 GEMV（`a19dbe87`）＝面向 8 投机验证行的小输出投影，作者在 2080Ti 实测。
- GGUF IQ2/IQ3 系列、Qwen4 加载峰值、AVX2 编译兼容 —— 与 GPU 生产路径无关。

## r9 合并与消解（2 处人工）
1. `fastllm-multicuda.cu`（3 区）：pick#734 初版 vs 上游定稿 → **取上游**（watchdog / fail-fast / 异步轮询全面取代）。
2. `qwen3_5.cpp`（1 区注释）：取**本地**文案（保 E1 构建标记；两侧守卫代码一致）。

## r9b（PR#747）合并与消解
- PR: `shenyan-008:contrib/qwen35-persistent-prefix` @ `3b46e6af`（base=2236e001）。内容：`qwen3_5_text` 纯文本 checkpoint 兼容、SSD 持久前缀缓存（复用 LMCache 原生 C++ FS 后端 + OpenSSL/SQLite）、启动指纹并行。
- 冲突 4 文件 8 区：
  - `include/models/basellm.h` / `qwen3_5.h`：**并集**（本地 `WantsPerStepPrefixSnapshot` + PR 持久化接口）。
  - `src/models/basellm.cpp`：**并集**（本地 PrefixDiag 打印 + PR `CanUsePagedPrefixCache` 守卫）。
  - `src/models/qwen3_5.cpp`（5 区）：swap 函数**保留本地 Impl+宏体系**（回退 PR 的 50 处显式命名调用为宏调用，保持 `张量名@行号` 诊断与 E4 标记）；快照记录处**并集**（diag + `persistentPrefixCache->Capture`）；注释保本地；多模态 Internal 拼接（本地 split 结构 + PR 的 `contextPrefill/prefixTokens` 语义）；多模态逐块记录**采用 PR 实现**（`ObservePersistentPrefill` + `st += curLen` + 页对齐记录，另加 `context != nullptr` 守卫）——**取代本地 e213ea10 逻辑**，需窗口图片请求回归。
- 构建注意：本机需 `-DCMAKE_EXE_LINKER_FLAGS=-static-libstdc++`（CUDA 链接排队引入 `-L/usr/lib/gcc/x86_64-linux-gnu/13`，该目录为老版 libstdc++.so、缺 `CXXABI_1.3.15`；gcc-14 编出的测试可执行文件否则链接失败。主 .so 不受影响）。
- 依赖：`apt install libssl-dev libsqlite3-dev`（3.5.7 / 3.46.1）；CMake 检出后启用 `FASTLLM_DISK_PREFIX_CACHE`。

## 验证（已完成）
- 构建：r9 / r9b 各约 5 分钟；markers 全绿（NCCL timeout/watchdog、bf16 8-row、守卫 E1–E4、无冲突标记）。
- 单测（build-r9b）：`qwen35_text_weights` / `disk_prefix_cache` / `multimodal_prefix_cache` / `paged_media_identity` **4/4 通过**（运行需 `LD_LIBRARY_PATH=<venv nccl>`）。
- overlay：`overlay-r9` / `overlay-r9b`（so 与构建一致；python 层含 PR 变更 + redact 重打）；`import ftllm` 通过。启动器 `fastllm_test_launch_r9.py` / `_r9b.py`（ARGV_FILE 选 ns/force）。

## 待执行
1. ~~**冒烟窗口**~~ **已执行**（见下节）：r9-force / r9b-force / r9b-ns 三探针全绿，md5 与 r8 基线逐位一致；生产自动恢复。
2. ~~**#747 功能深验**~~ **已完成（见下节 b）**：SSD 跨重启恢复 ✓；图片路径与现产逐项一致 ✓。
3. **转正流程**：窗口 + 验收通过后，r9 / r9b 择一转正（含与现产 Swift 行为的 A/B 复核）。

## 冒烟窗口结果（2026-09-21 10:39:49–10:42:16）

| 探针 | ping | count50 | arith_think | decode(t/s) |
|---|---|---|---|---|
| r9-force | f5de00c56dd1 | 30f8a5c9ee88 | 2adaf2269e77 | 228.3 / 202.1 |
| r9b-force | f5de00c56dd1 | 30f8a5c9ee88 | 2adaf2269e77 | 232.2 / 203.9 |
| r9b-ns | f5de00c56dd1 | 30f8a5c9ee88 | 2adaf2269e77 | 56.2 / 65.4 |

- 三探针 4/4 PASS ×3、errs=0；**三行 md5 与 r8 基线逐位一致**（f5de00c5 / 30f8a5c9ee88 / 2adaf2269e77），force 与 NS 跨配置一致。
- 生产自动恢复：PROD_READY ~30s；恢复后 so 仍 `1c7fc3f3`、`TP prepared: 5 paired MLPs` 在、0 Traceback；生产中断约 2 分钟。
- accept 行未出现（试跑 argv 未触发该日志）；证据：`ops/evidence/r9-747-20260921/window/`。

## 验收套餐结果（a，2026-09-21 10:50–11:03）

| 项 | 现产 r8（8080，同窗基线） | r9b（8081，同参配置） | 判定 |
|---|---|---|---|
| 功能探针 ×4 | md5 f5de00c5/30f8a5c9ee88/2adaf2269e77；dec 228.7/201.8 | 同 md5；dec 224.3/198.7（首窗）、231.2/198.9（同参窗） | ✓ 一致（窗口方差内） |
| 尾块 16K | decode 221.1 t/s | 212.9 / 218.2 t/s | ✓ 无回归 |
| 长文针测 308,204 tok | 09-19 生产记录：found ✓ 408s ≈755 tok/s | **found ✓（X9J7-QUARTZ-3312），预填 408.4s ≈754.6 tok/s** | ✓ 与生产记录一致 |
| 指纹 | — | seeded=2、desync=0、accept 100/98.44/95.31%、errs=0 | ✓ |

备注：首轮针测因试跑配置 max_context=262144 与 308K 提示不匹配而空答（配置问题非引擎），已按现产同参（1M+yarn，门控行在）重跑。

## b 深验：#747 功能（2026-09-21 11:04–11:08）

**1）SSD 持久前缀跨重启 e2e — ✓**
- 启动即算模型身份（identity=4b9cfc19…、format v2、quota 64GiB、restore=always；19.98GiB+3.58GiB 哈希 2.7s/2.7s）。
- 固定 16K 前缀：run1 冷 12.48s/cached 0 → run2 同进程 0.86s/cached 14336；提交检查点 10 个（8192…32768、14336）。
- 重启新进程：`[Prefix SSD] loaded/restored: tokens=14336（fp8→fp8，840ms）`；run3 3.94s/cached 14336/输出 md5 与冷跑一致（6ed2fc28eda2）；errs=0。
- 观测：部分写入被跳过（checkpoint_key_conflict，同键重复记录去重）。

**2）图片前缀路径回归 — ✓（与现产逐项一致，非回归）**
- r9b 与现产 r8 同探针：文字路径 T2/T3 命中（32768/34816）；图片会话 I2/I3 均 cached=0，**两版本行为完全相同**，原因是「含媒体复原跳过」保护（`[Qwen3.5 MM] prefix restore skipped: media tokens in delta`）按设计生效（视觉正确性优先）。
- r9b 的图片逐块快照（multimodal=1）正常记录（×48）；图像嵌入缓存命中；vision 编码正常。

## 风险与重评条件
- #747 体量大（+7373/−114，42 文件），含新数据路径；其自述 GPU 验收未在更新后的分支重跑。
- 多模态逐块记录改用 PR 实现：若图片前缀复用行为变化 → 重评条件 = 恢复本地实现（e213ea10）或调整记录条件。
- 若 shenyan-008 分支后续改稿，我们以合并时点 `3b46e6af` 为准。

## 证据
- `ops/evidence/r9-747-20260921/`：build status ×2、configure log ×2、ctest 输出、overlay so md5、resolve/linker 摘要、构建日志尾部。
- 上游参考：ztxz16/fastllm master `2236e001`；PR#747 head `3b46e6af`。

## 转正执行（2026-09-21 11:15–11:19）
- `switch_to_r9b.sh`：venv 备份 → 同步 overlay-r9b → 重启 → 功能回归检查 ×2：全 PASS（md5 同基线；dec 232.2/200.1 与 237.5/201.9）；errors=0。
- 部署态核验：venv so md5=66f68772…；`Prefix SSD`×8、`cache_sql_prepare` 均在 .so；`import ftllm.persistent_prefix` OK。
- 加验：产线 tail64=218.5 t/s、seeded=1、desync=0。
- 回滚：`ops/deploy/rollback_r9b.sh`（恢复备份 venv 后重启）。
- 生产状态：**r9b 已转正**；SSD 持久缓存功能在产线未开（env 未设，休眠）。

### SSD 持久前缀缓存产线启用（2026-09-21 11:30）
- `fastllm_prod_launch.py` 增 `FASTLLM_PREFIX_CACHE` 系列 env（脚本内注释含撤销方式；备份 .bak-pre-ssdprefix-20260921）。
- 复验：冷跑 11.79s → 跨重启 **2.31s/cached 14336/md5 一致**；回归 ×2 全绿；errors=0。
- 注：停服时后台提交最多等 30s（本次 14336 检查点在停服窗口内完成提交，故重启后可恢复到最长检查点）。
