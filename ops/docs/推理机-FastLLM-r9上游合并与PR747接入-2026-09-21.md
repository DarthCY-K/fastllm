# 推理机 FastLLM r9 上游全量合并 + PR#747 接入 — 2026-09-21

## 结论摘要
- **r9**（上游 13 提交全量）：分支 `r9-upstream-20260921` @ `15d53da0`；构建全绿，`so md5 ac56167602221ff4c5d8850930c00e07`。
- **r9b**（r9 + PR#747）：分支 `r9-747-20260921` @ `e5414da9`；构建全绿，`so md5 66f68772abe34a596bbe187addb0451a`；#747 自带 CPU 测试 4/4 通过。
- 冒烟窗口脚本已就绪（`ops/deploy/run_r9_window.sh`），**尚未执行**（等生产让位窗口；预计停产 12–16 分钟）。
- 生产未动：仍为 r8（so `1c7fc3f3`）。候选分支本地已提交；**推送 fork 待执行**（非交互会话无 GitHub 写凭据）。

## 上游内容（相对 r8 基线 cb465fe4，共 13 提交，master 2236e001）
- NCCL 启动自检定稿（`9be23bc6` + `2236e001`：fail-fast、超时 `FASTLLM_NCCL_INIT_TIMEOUT_MS`、回归测试）——取代我们 `3ad12f3d` 的初版 pick。
- BF16 八行小投影 GEMV（`a19dbe87`）＝面向 8 投机验证行的小输出投影，作者在 2080Ti 实测。
- GGUF IQ2/IQ3 系列、Qwen4 加载峰值、AVX2 编译兼容 —— 与 GPU 生产路径无关。

## r9 合并与消解（2 处人工）
1. `fastllm-multicuda.cu`（3 区）：pick#734 初版 vs 上游定稿 → **取上游**（watchdog / fail-fast / 异步轮询全面取代）。
2. `qwen3_5.cpp`（1 区注释）：取**本地**文案（保 E1 构建标记；两侧守卫代码一致）。

## r9b（PR#747）合并与消解
- PR: `shenyan-008:contrib/qwen35-persistent-prefix` @ `3b46e6af`（base=2236e001）。内容：`qwen3_5_text` 纯文本 checkpoint 兼容、SSD 持久前缀缓存（复用 LMCache 原生 C++ FS 后端 + OpenSSL/SQLite）、启动指纹并行。
- 冲突 4 文件 8 区：
  - `include/models/basellm.h` / `qwen3_5.h`：**并集**（本地 `WantsPerStepPrefixSnapshot` + PR 持久化接口）。
  - `src/models/basellm.cpp`：**并集**（本地 PrefixDiag 打印 + PR `CanUsePagedPrefixCache` 守卫）。
  - `src/models/qwen3_5.cpp`（5 区）：swap 函数**保留本地 Impl+宏体系**（回退 PR 的 50 处显式命名调用为宏调用，保持 `张量名@行号` 诊断与 E4 标记）；快照记录处**并集**（diag + `persistentPrefixCache->Capture`）；注释保本地；多模态 Internal 拼接（本地 split 结构 + PR 的 `contextPrefill/prefixTokens` 语义）；多模态逐块记录**采用 PR 实现**（`ObservePersistentPrefill` + `st += curLen` + 页对齐记录，另加 `context != nullptr` 守卫）——**取代本地 e213ea10 逻辑**，需窗口图片请求回归。
- 构建注意：本机需 `-DCMAKE_EXE_LINKER_FLAGS=-static-libstdc++`（CUDA 链接排队引入 `-L/usr/lib/gcc/x86_64-linux-gnu/13`，该目录为老版 libstdc++.so、缺 `CXXABI_1.3.15`；gcc-14 编出的测试可执行文件否则链接失败。主 .so 不受影响）。
- 依赖：`apt install libssl-dev libsqlite3-dev`（3.5.7 / 3.46.1）；CMake 检出后启用 `FASTLLM_DISK_PREFIX_CACHE`。

## 验证（已完成）
- 构建：r9 / r9b 各约 5 分钟；markers 全绿（NCCL timeout/watchdog、bf16 8-row、守卫 E1–E4、无冲突标记）。
- 单测（build-r9b）：`qwen35_text_weights` / `disk_prefix_cache` / `multimodal_prefix_cache` / `paged_media_identity` **4/4 通过**（运行需 `LD_LIBRARY_PATH=<venv nccl>`）。
- overlay：`overlay-r9` / `overlay-r9b`（so 与构建一致；python 层含 PR 变更 + redact 重打）；`import ftllm` 通过。启动器 `fastllm_test_launch_r9.py` / `_r9b.py`（ARGV_FILE 选 ns/force）。

## 待执行
1. **冒烟窗口**（`run_r9_window.sh`）：停生产 → r9-force / r9b-force / r9b-ns 三探针（trial_probe2；md5 对照 r8 基线 `f5de00c5 / 30f8a5c9ee88 / 2adaf2269e77`）→ 自动恢复生产。
2. **#747 功能深验**（后续窗口）：SSD 前缀跨重启 e2e（开关见 `docs/qwen35-persistent-prefix-cache.md`）；注意其自述限制：不支持 MoE/MTP/DSpark 的持久恢复组合；`auto` 策略无成本标定时跳过恢复。
3. **转正流程**：窗口 + 验收通过后，r9 / r9b 择一转正（含与现产 Swift 行为的 A/B 复核）。

## 风险与重评条件
- #747 体量大（+7373/−114，42 文件），含新数据路径；其自述 GPU 验收未在更新后的分支重跑。
- 多模态逐块记录改用 PR 实现：若图片前缀复用行为变化 → 重评条件 = 恢复本地实现（e213ea10）或调整记录条件。
- 若 shenyan-008 分支后续改稿，我们以合并时点 `3b46e6af` 为准。

## 证据
- `ops/evidence/r9-747-20260921/`：build status ×2、configure log ×2、ctest 输出、overlay so md5、resolve/linker 摘要、构建日志尾部。
- 上游参考：ztxz16/fastllm master `2236e001`；PR#747 head `3b46e6af`。
