# 推理机 chunk 组合窗与转正（chunk 2048 × interval 16 on LL128）— 2026-09-17

## 背景与假设
09-14 夜扫描曾测出 chunk=2048 使 c32 预填 −10.5%、c64 −5.6%（当时为 RING+LL 基座），未转正，
且明确留下"**未测：LL128 + chunk2048 组合**"。本窗补齐该组合并一次性完成转正验证。

## 窗口执行（16:32–16:54；staging = 生产本体代码）
- **staging 扫描器修正**：原 `scan_launch.py` 指向 `overlay-fix`（so=`9c010b44`，**陈旧**）→ 新建
  `scripts/scan_launch_chunk.py` 指向 venv site-packages（so=`70a04a15` = 生产）。v1 首跑因未携带
  `NCCL_PROTO=LL128` 被废并立即重开（部分数据留档 `window-part1-noll128.log`）。
- 配置：W0=LL128+512×i4（生产原样）｜**W1=LL128+2048×i16【候选】**｜W2=LL128+1024×i8｜W3=W1+NCCL_BUFFSIZE=16M。
- bench：w2k / d200_a / c32×3(w1,w2,m) / c64 / c128 / mt3 / d200_b，唯一 tag 冷请求 + 确定性 md5 校验。

## 结果（total 秒｜派生 tok/s）

| case | W0（生产基线） | W1（候选） | W2 | W3 |
|---|---|---|---|---|
| c32_m 预填 | 1181 t/s | **1277 t/s（+8.1%）** | 1258 | 1272 |
| c64 预填 | 1078 t/s | **1200 t/s（+11.3%）** | 1152 | — |
| c128 预填 | 926 t/s | **1073 t/s（+15.9%）** | — | — |
| d200 解码 | 217.5/215.2 t/s | 216.3/214.6 t/s（噪声内） | 216.1/214.7 | 215.9/214.8 |
| md5 | `dacc241c7db8` / `b536474ba172` 系 | **全同（无损）** | 全同 | 全同 |
| mt3 多轮 TTFT | t2 0.97 / t3 0.53s | t2 1.41 / t3 1.71s（**+0.4~1.2s/轮**） | — | — |

- **越长越赚**：c32 +8.1% → c64 +11.3% → c128 +15.9%（用户主场景=长文档预填）。
- 多轮粒度代价在预期内（复原点粒度 512→2048）；W2 中间档不占优；W3 BUFFSIZE 无增量（≈W1，噪声内）。
- 窗口期无降频：SM 均频 1822–1836 MHz、峰温 82°C。

## 转正（16:54:56–16:55:34）
- argv：`--chunked_prefill_size 512→2048`、`--prefix_cache_snapshot_interval_pages 4→16`。
- 备份 `results/argv-prod-tp4.json.bak-pre-chunk2048-20260917`；
  脚本 `ops/deploy/switch_to_chunk2048.sh`（含自动回滚）/ `rollback_chunk2048.sh` / `apply|rollback_chunk2048.py`。
- **转正后 prod probe**：c32 26.03s（=W1 水平，较转正前 28.07s **−7.2%**）、d200 3.30s、
  md5 全同；`prod_check` 五连全绿（count200 **215.7 t/s**，md5 零漂移）。

## 诊断税清理（B）
- `systemctl unset-environment FT_CUDA_ALLOC_TRACE FT_CUDA_ALLOC_TRACE_MIN_MB FT_PROMPT_FPRINT` + 重启；
  probe 前后对比无量化差异（±0.05s）→ **纯卫生收益**（去掉每请求 FPRINT 日志与 ≥1MB 分配追踪风险）。
- 保留 `FT_TOOLCALL_DEBUG_OUTPUT=1`（工具块取证用，有意）。进程 env 复核见 `proc-env-after.txt`。

## 未做 / 下一档
- chunk 4096×i32 未测（趋势外推未知；粒度代价将进一步增大 ~+2s/轮）。
- 解码旋钮级已穷尽；下一个台阶在引擎级（decode graph/通信重叠）。

## 证据索引（`ops/evidence/chunk-window-20260917/`）
`compare-all.txt`（六列全表）｜`chunkwin_W0..W3.json` + `stack-W*.log` + `chunkwin_clocks_W*.log`｜
`post-chunk2048-probe.json` / `postB-probe.json` / `post-chunk2048-prodcheck.txt`｜
`switch-chunk2048-run.log`｜`ft-before.txt` / `ft-after-unset.txt` / `proc-env-after.txt`｜
`window-run.log` / `window-part1-noll128.log`（v1 废跑留档）。
